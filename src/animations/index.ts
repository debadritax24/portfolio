/**
 * Centralized animation variants.
 * Import from @/animations instead of duplicating variants across sections.
 */
export { fadeIn, fadeInUp, fadeInUpWithDelay } from "./fade";
export {
  staggerContainer,
  staggerItem,
  createStaggerContainer,
} from "./stagger";
